Footer: org identity, optional contact, one-line legal note. One per page.

```jsx
<Footer />
```

Phone/email columns only render when passed, keeping the footer lean until real contact info is confirmed.
