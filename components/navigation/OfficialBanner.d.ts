/**
 * Thin identity strip above the header: a recurring, actionable reminder (board meeting time + Zoom link) rather than a static legal statement.
 */
export interface OfficialBannerProps {
  text?: string;
  zoomHref?: string;
}
