/**
 * Dashed-border, monospace note marking content that is not yet final (pending copy, confirmation, or rights) so it's never mistaken for live content.
 */
export interface PlaceholderNoteProps {
  children: React.ReactNode;
}
