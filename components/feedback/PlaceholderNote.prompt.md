PlaceholderNote: flags unfinished content (pending board approval, missing rights, TBD info) so it reads clearly as a placeholder, not real copy.

```jsx
<PlaceholderNote>placeholder: current Zoom link, meeting ID and passcode, confirm before publishing</PlaceholderNote>
```
